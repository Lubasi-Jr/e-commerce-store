import React from "react";

const TestBanner = () => {
  return (
    <div>
      This is a test banner that should only show on the Log In and Sign Up page
    </div>
  );
};

export default TestBanner;
